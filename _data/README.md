# `_data/roadmap.json`

Generated file — do not hand-edit.

Source of truth: `corporate/src/data/poesis-portfolio.ts` (each feature carries its own
version milestone; the timeline per product is derived from those, plus a synthesized
"In active development" entry for a pre-GA `currentVersion` that has no feature yet).

Regenerate after any change to `poesis-portfolio.ts`:

```bash
cd corporate && npm run sync-roadmap
```

Rendered by `_includes/roadmap_timeline.html` on `milestones.md`.
