# Jekyll build artifacts
_site/
.jekyll-cache/
.jekyll-metadata
vendor/

# GSM spec pages are fetched from the gsm repo (poesis-cloud/gsm) at build time —
# it is the single source of truth. Only the section landing stays in this repo.
/gsm/*.md
!/gsm/index.md
