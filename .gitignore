# Jekyll build artifacts
_site/
.jekyll-cache/
.jekyll-metadata
vendor/

# GSM docs are maintained in this repository (gsm repo is private; Pages cannot
# fetch it at build time). Keep the full GSM section published with the site.
